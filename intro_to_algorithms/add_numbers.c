#include <stdlib.h>
#include <stdio.h>

void main(void) {
    // Declare two vars to sum
    int a = 5;
    int b = 4;

    // Sum the two variables
    int result = a + b;

    // print the result
    printf("%d + %d = %d \n", a, b, result);

}